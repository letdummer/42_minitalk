#include "../libft/libft/libft.h"
#include "../libft/ft_printf/libftprintf.h"
#include <sys/types.h>
#include <unistd.h>
#include <stdio.h>

 int	main()
{
/*	pid_t	pid;

	pid = getpid(); // get the Process ID. Returns the found value

	//printf("pid: %lu\n", pid);	// ATUALIZAR O PRINTF PARA UTILIZAR A FLAG LU 
	// ou tentar da forma abaixo:
	ft_printf("pid: %d\n", (int)getpid());
*/
	printf("okkkk\n");
} 